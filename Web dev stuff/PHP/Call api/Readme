This is designed to call the api featured in C#/Election_Api-Demo.zip

If you are running the standalone:
Just run it, it will be fine.

If you are using Callapi_fetch.php:
Run check_code_and_con.
Enter a 10 char code in the code input box (default passcode is letmein123).
Enter a number between 1 and 999 in the con input box (currently 0-65 are valid).
Press the submit button to action the form.
Callapi_fetch will be called and run to completion.
